import { withMixin } from "../../../helpers/dva";

const init = {};
export default withMixin({
  namespace: "ShiMin",
  state: {
    ...init
  },
  subscriptions: {},
  effects: {},
  reducers: {}
});
