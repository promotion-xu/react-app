import { withMixin } from "../../../helpers/dva";

const init = {};
export default withMixin({
  namespace: "XuZhen",
  state: {
    ...init
  },
  subscriptions: {},
  effects: {},
  reducers: {}
});
