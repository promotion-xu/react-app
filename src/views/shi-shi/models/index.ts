import { withMixin } from "../../../helpers/dva";

const init = {};
export default withMixin({
  namespace: "ShiShi",
  state: {
    ...init
  },
  subscriptions: {},
  effects: {},
  reducers: {}
});
