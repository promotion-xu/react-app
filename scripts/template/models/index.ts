import { withMixin } from "../../../helpers/dva";

const init = {};
export default withMixin({
  namespace: "template",
  state: {
    ...init
  },
  subscriptions: {},
  effects: {},
  reducers: {}
});
